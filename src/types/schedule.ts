export type ScheduleEntry = {
    id: number
    userId: string
    name: string
    date: Date
    startTime: string
    endTime: string
    title: string
    location: string
    description: string
}
